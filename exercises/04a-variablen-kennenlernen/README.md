# Variablen kennenlernen

Variablen speichern die Daten, damit wir den Daten einen Namen können und später referenzieren oder sogar manipulieren
können.

## Ziel

Folgende Konzepte kennengelernt
1. Variablen zum Speichern von Daten
2. Variablen deklarieren mit `var` und `:=`

## Code

{{Code}}

2. Downloade den Code mit:   

## Erinnerung

```
<Variablen Name> := <Wert>

var <Variablen Name> <Typ>

var <Variable>, <Variable> = <Wert>, <Wert>
```
